# 🛠️ Настройка на GitHub Metrics Dashboard

## Бързо начало

### 1. Създай ново репозитори

```bash
# Клонирай или копирай файловете
git clone <this-repo> github-metrics
cd github-metrics
```

### 2. Създай Personal Access Token (PAT)

1. Отвори **GitHub Settings** → **Developer settings** → **Personal access tokens** → **Fine-grained tokens**
2. Натисни **Generate new token**
3. Настройки:
   - **Name**: `metrics-dashboard`
   - **Expiration**: избери подходящ период (препоръчвам 1 година)
   - **Repository access**: `All repositories` или избери конкретни
   - **Permissions**:
     - `Contents`: Read
     - `Metadata`: Read
4. Копирай генерирания token

### 3. Добави secrets и variables

В твоето ново репо отиди на **Settings** → **Secrets and variables** → **Actions**

#### Secrets
| Име | Стойност |
|-----|----------|
| `METRICS_TOKEN` | Твоя Personal Access Token |

#### Variables
| Име | Стойност |
|-----|----------|
| `GH_USERNAME` | Твоето GitHub username |
| `REPOS_TO_TRACK` | (опционално) Списък с репота, разделени със запетая |

**Пример за REPOS_TO_TRACK:**
```
owner/repo1,owner/repo2,organization/project
```
Ако оставиш празно, ще се следят всички достъпни репозиторита.

### 4. Активирай GitHub Pages

1. **Settings** → **Pages**
2. **Source**: `GitHub Actions`
3. Запази

### 5. Стартирай първото обновяване

1. Отиди на **Actions** таба
2. Избери **Update Code Metrics**
3. Натисни **Run workflow** → **Run workflow**

## 📂 Структура на проекта

```
github-metrics-dashboard/
├── .github/workflows/
│   └── update-metrics.yml    # GitHub Action за автоматизация
├── scripts/
│   ├── collect_metrics.py    # Събира данни от GitHub API
│   └── generate_dashboard.py # Генерира HTML dashboard
├── data/
│   └── metrics.json          # Текущи метрики (auto-generated)
├── docs/
│   └── index.html            # HTML dashboard (auto-generated)
├── README.md                 # Таблица с метрики (auto-generated)
├── SETUP.md                  # Този файл
└── .gitignore
```

## ⚙️ Конфигурация

### Промяна на честотата на обновяване

Редактирай `.github/workflows/update-metrics.yml`:

```yaml
on:
  schedule:
    # Всеки ден в 6:00 UTC
    - cron: '0 6 * * *'
    
    # Или: на всеки 6 часа
    # - cron: '0 */6 * * *'
    
    # Или: всяка седмица в понеделник
    # - cron: '0 6 * * 1'
```

### Следене на конкретни репозиторита

Добави variable `REPOS_TO_TRACK` със списък:
```
myusername/project1,myusername/project2,myorg/shared-project
```

### Промяна на периоди

Редактирай `scripts/collect_metrics.py` в секция `periods`:
```python
periods = {
    'today': (now.replace(hour=0, minute=0, second=0), now),
    'week': (now - timedelta(days=7), now),
    'month': (now - timedelta(days=30), now),
    # Добави свои периоди...
}
```

## 🔍 Отстраняване на проблеми

### Action не стартира
- Провери дали **Actions** са активирани за репото
- Провери дали `METRICS_TOKEN` е добавен правилно

### Няма данни за някои репозиторита
- Провери permissions на PAT
- Репото може да е празно или без commits от теб

### GitHub Pages не показва нищо
- Изчакай няколко минути след първия deploy
- Провери **Settings** → **Pages** за грешки

### Rate limiting
- GitHub API има лимит от 5000 requests/час
- При много репозиторита, може да се наложи оптимизация

## 📊 Достъп до Dashboard

След успешен deploy, dashboard-ът ще е достъпен на:
```
https://<username>.github.io/<repo-name>/
```

## 🔐 Сигурност

- **Никога** не commit-вай PAT директно в кода
- Използвай GitHub Secrets за чувствителни данни
- Преглеждай периодично permissions на token-а
- Token-ът има достъп само за четене

## 📝 Лиценз

MIT - свободен за използване и модификация.
